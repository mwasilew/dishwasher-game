Not a dependency tree — these are three-line re-export shims so `node test/smoke.mjs`
resolves the same bare specifiers ("three", "cannon-es") that the browser resolves via
the importmap in index.html. The real libraries live in ../vendor/. Nothing here is
needed to play the game.
